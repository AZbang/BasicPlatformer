import Entity from './Entity'

export default class Slime extends Entity {
  constructor(objects, x, y, w, h, properties) {
    super(objects, x, y, w, h, properties);

  }
  updateBehavior(dt) {

  }
  onCollide(obj) {
    
  }
}
